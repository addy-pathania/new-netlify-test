{
  "name": "angular-xmc",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd ./proxy && npm run build-all && cd ..",
   "install": "cd ./proxy && npm install && npm run install-all && cd .."
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
